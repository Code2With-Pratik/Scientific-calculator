# 🧮 Modern Scientific Calculator

![Calculator Screenshot](image.png)
A sleek, dark-themed scientific calculator designed for performing both basic arithmetic and advanced mathematical functions. This project features a responsive user interface with a modern color palette and intuitive button layout.

## 🚀 Demo

[**View Live Demo**](https://code2with-pratik.github.io/Scientific-calculator/)
## ✨ Features

Based on the interface design, the calculator includes the following functionalities:

* **Standard Arithmetic**: Addition (`+`), Subtraction (`-`), Multiplication (`×`), and Division (`÷`).
* **Scientific Functions**:
    * Trigonometry: Sine (`sin`), Cosine (`cos`), Tangent (`tan`).
    * Logarithms: `log`.
    * Roots & Powers: Square root (`√`), Power (`^`), Exponential (`exp`).
* **Constants**: Pi (`π`) and Euler's number (`e`).
* **Input Management**:
    * `AC`: All Clear (Resets the calculation).
    * `DEL`: Delete (Removes the last entered digit).
    * Parentheses `( )` for order of operations.
* **Modern UI/UX**:
    * Eye-friendly **Dark Mode** design.
    * Color-coded buttons for easy distinction (Pink for AC, Green for Equals, Orange for Operators).
    * Responsive grid layout.

## 🛠️ Tech Stack

* **Frontend**: HTML5, CSS3
* **Logic**: JavaScript (ES6+)
* **Styling**: Flexbox / CSS Grid (for the keypad layout)
* **Font**: Sans-serif typography for clean readability

## 📂 Project Structure

```bash
├── index.html      # Main HTML structure
├── style.css       # Styling for dark theme and buttons
├── script.js       # Calculation logic and event listeners
├── README.md       # Project documentation
└── assets/         # Images and icons